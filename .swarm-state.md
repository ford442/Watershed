# Swarm state — atomic deploy + build identity (#416)

## Phase E — 2026-09-16 — complete — HEAPF32 views survive memory growth

**Objective:** Rebind HEAPF32 views at the three listed sites; tests simulate growth; pick strategy from a benchmark.

### #415 verification (do not trust the issue state)
GitHub issue #415 is **closed completed** (2026-09-15) with `closed_by_pull_requests.total_count = 0`. Tree before this phase still captured views once. This phase implements the three in-scope sites only.

### Files changed
| File | Notes |
|---|---|
| `src/systems/water/WatershedWasm.ts` | `heapF32()`; SWE getters; water-force batch refresh |
| `src/physics/physicsWorkerWaterForces.ts` | `refreshPhysicsWorkerWaterBatch` at tick start |
| `src/systems/water/WatershedWasm.test.ts` | growth tests |
| `src/physics/__tests__/physicsWorkerWaterForces.heap.test.ts` | new |
| `verification/heapf32_bench.mjs` | new |

### Benchmark (200_000 iters, this machine)
| Strategy | ns/op |
|---|---|
| always-new 8-float | 123.2 |
| **heapF32 lazy 8-float** | **98.0** ← used on the frame-hot water-force path |
| always-new 32×32 SWE | 95.4 |
| heapF32 lazy 32×32 SWE | 135.3 |
| heapF32 after simulated growth | 239.8 |

Lazy wins on the 8-float path. SWE keeps lazy getters so `grid.h[i]` in `WaterForceSystem` (untouched) rebinds on growth; 40 ns/op either way is noise vs the solver.

### Checks
| Check | Result |
|---|---|
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 — **0 errors, 296 warnings** (back to baseline after removing one unused `rel`) |
| `pnpm test` | **882 passed / 5 skipped** |
| `node emscripten/smoke_test.mjs` | abi=8 |
| markdown paths | 29 files validated |

### Leftover #415 surface (out of this swarm's file list)
`SplashSystem.tsx` SoA views, `WaterfallParticles.tsx`, `gpuChores/watershedHost.ts`. Owner may reopen #415 for those or file a follow-up.

### BLOCKED — needs owner
Same as Phase A: `DEPLOY_TOKEN` unset. **Closing #416 is Noah's step:** authenticated `python3 deploy.py`, then `curl -sSI https://test.1ink.us/watershed/` (directory URL) must be UTF-8 with current `build-id`, assets named by that document exist in the same `build/`, glue/wasm sizes match HEAD as a pair, entry bundle contains `WasmInitTimeoutError` and `negotiateBootGraphics`.

### Next
None for this swarm. Owner: `--list-remote` with token, then real deploy, then close #416 on the external fetch.

---

## Phase D — 2026-09-16 — complete — verify_deploy seen to fail and to pass

**Objective:** `verification/verify_deploy.mjs` fetches the **directory URL**, fails today's live server, passes a coherent local `build/`.

### Files
`verification/verify_deploy.mjs`, `verification/verify_deploy.test.mjs` (4 tests), `package.json` script `verify:deploy`.

### Evidence — FAIL against live (`node verification/verify_deploy.mjs --skip-zip`)

```
Fetching DIRECTORY URL  https://test.1ink.us/watershed/
directory encoding=utf-16le bytes=1438
FAIL  directory URL is not UTF-8 (got utf-16le, 1438 bytes)
FAIL  directory URL carries no <meta name="build-id">
FAIL  asset … index-8Gl23hpQ.js absent from build/
FAIL  watershed_native.js size live=26999 HEAD/build=27029
FAIL  watershed_native.wasm size live=33817 HEAD/build=33840
FAIL  served entry bundle missing WasmInitTimeoutError
FAIL  served entry bundle missing negotiateBootGraphics
VERDICT: NOT LIVE  (12 check(s) failed)
```

### Evidence — PASS against local static server

`python3 -m http.server 4180 --directory build` then
`node verification/verify_deploy.mjs --url http://127.0.0.1:4180/ --skip-zip`

```
directory encoding=utf-8 bytes=812
ok    build-id matches 61c6ce9 4d5ade0f0dc990a0 2026-09-16T22:28:53.069Z n129
ok    glue/wasm pair sizes both equal HEAD/build
ok    entry bundle contains WasmInitTimeoutError
ok    entry bundle contains negotiateBootGraphics
VERDICT: LIVE
```

A verifier that has never been seen to fail is not a verifier. Both transcripts recorded.

### Checks (this phase, before E)
typecheck 0 / lint 0 errors 296 warnings / tests include +4 verify_deploy predicates.

---

## Phase C — 2026-09-16 — complete — atomic public passengers + boot stamp assert

**Objective:** Size-skip only Vite hashed `assets/*`; glue/wasm are one unit; `--dry-run` lists every upload with sizes and warns when the sizes endpoint is down; boot hashes served glue+wasm against `WASM_ARTIFACT_STAMP`.

### Files changed
| File | Notes |
|---|---|
| `deploy.py` | hashed-asset skip; `NATIVE_PAIR` force-upload invariant; `--manifest`; dry-run per-file sizes + WARNING on 403 |
| `src/systems/water/WatershedWasm.ts` | `hashGlueWasmPair` + `assertLoadedArtifactStamp` after ABI check |
| `src/systems/water/WatershedWasm.test.ts` | +3 tests (mismatch rejects, match resolves, hasher stable) |
| `emscripten/build.sh` | stamp written for `--threads` too (still glue+wasm only) |

### Checks
| Check | Result |
|---|---|
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 — **0 errors**, 296 warnings |
| `pnpm test` | **873 passed / 5 skipped** |
| `pnpm exec vite build` | already green this session (index.html 820, BUILD_ID present) |
| `node emscripten/smoke_test.mjs` | `abi=8` |

Python `hashlib.sha256(glue+wasm).hexdigest()[:16]` == `4d5ade0f0dc990a0` == `WASM_ARTIFACT_STAMP`.

### Evidence — `python3 deploy.py --dry-run` (token unset, sizes HTTP 403)

```
WARNING: sizes endpoint unreachable (HTTP 403). Skip set is empty;
  this dry-run lists a full upload and does not match an authenticated
  run's skip set. DirectoryIndex clones cannot be computed either.
```

Would upload 52 files including:

```
          55  BUILD_ID
         820  index.html
       27029  watershed_native.js
       33840  watershed_native.wasm
     1796857  assets/index-CPn7khiA.js
```

Glue 27029 and wasm 33840 both listed (pair). Unhashed passengers always `+`, never `=`.

### BLOCKED — needs owner
Same token. An authenticated dry-run is the first time skip-vs-upload is real; the 403 fallback still lists everything.

### Next
**Phase D** — `verification/verify_deploy.mjs` fetching `https://test.1ink.us/watershed/` (directory URL). First step: write the script so it fails on today's UTF-16 directory doc.

---

## Phase B — 2026-09-16 — complete — BUILD_IDENTITY that cannot be size-invariant

**Objective:** Stamp a per-build identity into the JS bundle, `index.html` `<meta name="build-id">`, and unhashed `build/BUILD_ID`; log it at WASM init.

### Files changed
| File | Notes |
|---|---|
| `src/buildIdentity.ts` | new, 6 lines — `BUILD_IDENTITY` from `__WATERSHED_BUILD_IDENTITY__` |
| `vite.config.ts` | `define` + `transformIndexHtml` + `closeBundle` writes `build/BUILD_ID` |
| `index.html` | `<meta name="build-id" content="__WATERSHED_BUILD_ID__" />` |
| `src/systems/water/WatershedWasm.ts` | logs `[Watershed WASM] build-id=...` next to init started |

Identity string: `<shortSha> <WASM_ARTIFACT_STAMP> <ISO> n<0-255>`. The trailing nonce is 1–3 digits because ISO-8601-with-ms is fixed-width (two consecutive Vite builds both produced **807** then both **820** bytes until the nonce). Phase C still never size-skips `index.html`.

### Checks
| Check | Result |
|---|---|
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 — **0 errors**, 296 warnings |
| `pnpm test` | **870 passed / 5 skipped** — logs show `build-id=61c6ce9 4d5ade0f0dc990a0 2026-09-16T22:15:51.603Z` |
| `pnpm exec vite build` | exit 0; `build/index.html` 820+ bytes with meta; `build/BUILD_ID` matches meta |
| `node emscripten/smoke_test.mjs` | `abi=8` |

### Evidence
`build/index.html` (first identity build):

```html
<meta name="build-id" content="61c6ce9 4d5ade0f0dc990a0 2026-09-16T22:16:57.994Z" />
```

`build/BUILD_ID` identical. UTF-8, no BOM. Meta match True. Pre-identity `index.html` was 718 bytes; with meta it is 807–820+.

`curl -sS https://test.1ink.us/watershed/BUILD_ID` will answer "what is live?" after Noah's deploy. Not live yet.

### BLOCKED — needs owner
Same as Phase A: no `DEPLOY_TOKEN`. Identity is local-only until an authenticated deploy.

### Next
**Phase C** — skip only hashed `assets/*`; glue/wasm pair invariant; honest `--dry-run`; boot stamp assert vs `WASM_ARTIFACT_STAMP`.

First concrete step: replace `build_zip` size-skip predicate.

---

## Phase A — 2026-09-16 — complete — name the shadow, own the directory URL

**Objective:** `deploy.py --list-remote` prints the remote size map (overlap vs orphans); a deploy clones UTF-8 `index.html` onto remote-only DirectoryIndex candidates so `/watershed/` is the pipeline's responsibility.

### Files changed
| File | Lines (this pass) |
|---|---|
| `deploy.py` | 390 lines total; +200 / −15 vs previous |

`--list-remote` groups the sizes map into (a) paths also in `build/` and (b) remote-only orphans with a running byte total, then lists DirectoryIndex shadow candidates (root size 1438, `index|default|home|welcome*.htm(l)`, any other remote-only root `*.htm(l)`). `build_zip` never size-skips `index.html`. When the sizes map is present, extra zip members clone `build/index.html` bytes onto those shadow paths.

### Checks
| Check | Result |
|---|---|
| `pnpm typecheck` | exit 0 — 0 untyped modules; systems-layout ok; glsl-hosts 13/1 residual |
| `pnpm lint` | exit 0 — **0 errors**, 296 warnings (backlog; not grown by this phase) |
| `pnpm test` | **870 passed / 5 skipped** (875 total; baseline in the brief was 827/5 — this branch already had more tests) |
| `pnpm build` | exit 0. `emcc 6.0.3 != pinned 3.1.56` warning (pre-existing skip-unless-require path); stamp stayed `4d5ade0f0dc990a0`, glue 27029 / wasm 33840, `public/` not dirtied |
| `node emscripten/smoke_test.mjs` | `watershed_native smoke ok (buoyancy=6258.66 abi=8)` |

### Evidence
`--list-remote` with no token (expected):

```
=== Remote listing for 'watershed' ===

BLOCKED — needs owner: DEPLOY_TOKEN is not set.
Export the deploy token and re-run:
  python3 deploy.py --list-remote
The 1438-byte root HTML file is the DirectoryIndex shadow that
https://test.1ink.us/watershed/ actually serves.
```

exit 1. No fake listing. Sizes GET is not attempted without a token.

`--dry-run` (sizes HTTP 403 fallback): 52 files, 9001.5 KB, includes `index.html` and `watershed_native.{js,wasm}`. No DirectoryIndex clones (size map unavailable). Printed:

```
! sizes map unavailable — DirectoryIndex clones cannot be computed.
  index.html will still upload; the UTF-16 shadow will not be overwritten
  until --list-remote succeeds and a deploy re-runs with a size map.
```

Live headers reconfirmed this session: `/watershed/` 1438 UTF-16LE `ff fe` mtime 12:21:30; `/watershed/index.html` 718 UTF-8 mtime 12:21:58. Guessed names (`index.htm`, `default.htm`, `default.html`, `Index.html`, ~30 others) all 404.

### BLOCKED — needs owner

`DEPLOY_TOKEN` is unset in this sandbox (expected). Owner action that unblocks naming the shadow:

1. `export DEPLOY_TOKEN="<token from VPS env>"` (do not commit it).
2. `python3 deploy.py --list-remote`
3. Find the 1438-byte **root** HTML file. Confirm with `curl -sSI https://test.1ink.us/watershed/<name>` — `last-modified` must be Fri, 14 Aug 2026 12:21:30, length 1438, body starts `ff fe`.
4. Authenticated `python3 deploy.py` so that path is overwritten with UTF-8 `index.html` bytes.

Closing #416 still requires that authenticated deploy plus an external fetch of the **directory URL**. Not this swarm's step.

### Next
**Phase B** — `BUILD_IDENTITY` in `vite.config.ts` `define`, `src/buildIdentity.ts`, `<meta name="build-id">` in the Vite template, `build/BUILD_ID` unhashed text file, log once from `WatershedWasm.ts` beside `[Watershed WASM] init started`.

First concrete step: read `WASM_ARTIFACT_STAMP` from `src/systems/water/wasmArtifactStamp.ts` and wire `__WATERSHED_BUILD_IDENTITY__`.

---

## Iteration 2 — 2026-08-13T01:15:00Z — complete

### Batch: fauna → flora → water → weather → composites (all 38 modules)

**Converted (.jsx → .tsx):**
- Fauna: `Bats`, `Birds`, `Dragonflies`, `Fireflies`, `Fish`
- Flora: `Cactus`, `DesertSage`, `Ferns`, `Foliage`, `Grass`, `Mushrooms`, `Reeds`, `Wildflowers`, `CanyonGrass`, `Vegetation`
- Water-adjacent: `Rapids`, `RockFoam`, `WaterLilies`, `WaterfallImpactZone`, `WaterfallParticles`, `WaterfallSheet`, `Driftwood`, `FloatingDebris`
- Weather/atmos: `Mist`, `IceSpray`, `Icicles`, `FallingLeaves`, `SunShafts`, `Rainbow`, `CanyonDust`, `IceSheets`, `CanyonBackground`, `Pebbles`, `Pinecone`
- Composites: `CanyonDecorations`, `PooledObstacles`, `TreeSystem`, `VFX/SplashParticles`

### Allowlist / typecheck surface
| Metric | Value |
|--------|-------|
| `scripts/untyped-allowlist.json` length | **0** |
| `tsconfig.typecheck.json` `.jsx` exclude | **removed** |
| Shared props | `src/components/Environment/types.ts` (`BiomeDecorationProps`, component-specific props) |

### Schema debt
| Item | Status |
|------|--------|
| `KNOWN_LEVEL_SCHEMA_DEBT` | **[]** |
| `level.schema.json` | `decorations` optional; segment `index` min **-10** (pre-roll); `assertLevelData` uses AJV only |
| `meander_to_waterfall.json` | segment 13 explicit `decorations: {}` |

### Checks (final)
| Check | Status |
|-------|--------|
| `pnpm typecheck` | ✅ exit 0 |
| `pnpm test` | ✅ 533 passed, 2 skipped |
| `pnpm build` | ✅ exit 0 |

### Also touched (ride-along)
- `package.json` — `"type": "module"`
- `AGENTS.md` — typed-surface table updated

### Next batch
None — arc complete. P1 content wave can inherit `BiomeDecorationProps` from `Environment/types.ts`.

---
## #371 Phase A — 2026-08-19 — complete

- Archived: src/PLAN.md, src/systems/PLAN.md, src/components/PLAN.md → docs/archive/src-*-PLAN.md
- README roadmap: unchecked only #369 #370 #372 #373 #374 #375
- CODE_HEALTH_GUIDE: VehicleMount mount diagram; quiet-boot golden rule
- typecheck: 0 | test: 0 (644 passed) | root systems/*.ts count: 79
- Deferred: none (docs-only)
- Next: Phase B — namespace src/systems/ into domain folders + CI guard

---
## #371 Phase B — 2026-08-19 — complete

- Namespaced systems into journey/ survival/ ghost/ water/ reach/ score/ audio/ map/ persistence/ settings/ pools/ vehicle/ lighting/
- Thin systems/index.ts (Biome/LOD/Splash/GameState only); removed PostProcessing export
- Added scripts/check-systems-layout.mjs; wired into pnpm typecheck
- typecheck: 0 | test: 0 (644 passed) | root systems/*.ts count: 79
- Deferred root tsx: BiomeSystem, LODManager, SplashSystem (+ PostProcessing until Phase D)
- Next: Phase C1 — split RunnerPhysicsStep.ts under 650 lines

---
## #371 Phase C — 2026-08-19 — complete

- RunnerPhysicsStep 601 (runnerGroundRay + runnerAirControl)
- FlowingWater 627 (waterUniforms.ts)
- BiomePalettes 26 facade + configs/palettes/*
- TrackSegmentMeshes 576 (collision + decorations + propLayouts)
- ChunkManager 550 (chunkForecastApply.ts)
- typecheck: 0 | test: 0 (653 passed) | root systems/*.ts: 79
- Deferred: BiomeSystem/LODManager/SplashSystem still at root; PostProcessing until D
- Next: Phase D — delete PostProcessing dual; strip GameHUD/TrackManager/MapSystem debug

---
## #371 Phase D — 2026-08-19 — complete (DoD)

- Deleted src/systems/PostProcessing.tsx; CI no longer allows it
- GameHUD: silent WASM smoke (panel kept, no console.info/warn)
- TrackManager: removed __watershedMapInfo + Map binding confirmed log
- MapSystem: removed ProceduralMapManager bound console.log only
- typecheck: 0 | test: 0 (653 passed) | root systems/*.ts: 80
- Deferred (intentional): BiomeSystem.tsx, LODManager.tsx, SplashSystem.tsx at systems root
- DoD: navigation honest — no lying PLAN.md under src; systems namespaced; hosts <650; duals/debug gone
- Next: none for #371 (ready for PR / close issue)

---
## #402 WASM init hang — 2026-09-02T18:30:00Z — complete

### Phase 0 — Reproduce (iteration 1)

**Headless preview probe** (`pnpm build` + `pnpm preview` + Puppeteer/SwiftShader):
- Start-menu visual-smoke **passed** (required gate `00_start_menu`).
- Page reaches canvas + DOM (`rootChildCount=3`) but **no `[Watershed WASM]` console lines** pre-fix (logging did not exist).
- `wasm-smoke-status` / `wasm-init-banner` not present on start menu (GameHUD `Html` overlay needs in-game WebGL boot; start button stayed disabled >120s in headless asset prep).
- **Deploy hang not reproduced headlessly** — proceed on static audit: unbounded `factory()` await, `_initError` only in catch, singleton parks all six call sites.

### Phase 1–2 — Deadline + observability

| File | Change |
|------|--------|
| `src/systems/water/WatershedWasm.ts` | `WASM_INIT_TIMEOUT_MS=8000`, `resolveWasmInitTimeoutMs`, `WasmInitTimeoutError`, `raceWithDeadline`, structured `[Watershed WASM]` logs, reject-on-failure singleton |
| `src/physics/workerWasm.ts` | Same deadline; resolves `null` on timeout/failure (worker contract) |
| `src/components/GameHUD.tsx` | Distinguish timeout vs throw banner text |
| `src/components/GameHUD.test.tsx` | Timeout banner case; partial mock via `importActual` |
| `src/systems/water/WatershedWasm.test.ts` | `resolveWasmInitTimeoutMs` + deadline rejection tests |

**Promise shape:** `getWasm()` **rejects** on failure; `getWorkerWasm()` **resolves null**. All six call sites already `.catch()` — no call-site edits required.

### Phase 3 — Memory ceiling

| File | Change |
|------|--------|
| `emscripten/CMakeLists.txt` | `MAXIMUM_MEMORY=256 MiB` (kept `INITIAL_MEMORY=64 MiB`) |
| `WASM.md` | Linear memory budget table + init deadline cross-ref |
| `docs/reference/WASM.md` | Cross-ref to memory budget |

**ASSERTIONS (Release):** kept `ASSERTIONS=0` — no rebuild size delta measured; timeout + `MAXIMUM_MEMORY` are the primary guards.

### Phase 4 — ABI 8 rebuild (emcc present)

- `pnpm build:wasm` → `public/watershed_native.{js,wasm}`, stamp `4d5ade0f0dc990a0`, `getVersion()===8`
- `emscripten/smoke_test.mjs` — floor ABI 8, `applySWEEvent` vs TS twin parity
- Header comment fixed (currently 8)

### Checks (final)

| Check | Status |
|-------|--------|
| `pnpm typecheck` | ✅ exit 0 |
| `pnpm test` | ✅ 827 passed, 5 skipped |
| `pnpm build` | ✅ exit 0 |
| `pnpm test:visual-smoke` | ✅ required gate passed; baselines unmoved |
| `node emscripten/smoke_test.mjs` | ✅ `abi=8` |
| `pnpm test:native` | ✅ `watershed_host_smoke ok` |
| `WATERSHED_WASM_INTEGRATION=1 pnpm test:wasm` | ✅ 2 passed |

### Behavioural timeout proof

- **Unit:** `getWasm init deadline` test — 50ms env override, never-settling factory → `WasmInitTimeoutError`, `peekWasmInitError()` set, `[Watershed WASM] timed-out(50ms)`.
- **HUD:** `GameHUD.test.tsx` — timeout banner text distinct from throw.
- **Browser headless:** `?wasmInitTimeout=1` + Start click blocked (start button disabled 120s waiting on assets in SwiftShader). Deferred to unit/integration coverage above.

### Deferred

- Release `ASSERTIONS=1` size measurement (low priority; timeout is primary fix).
- In-browser `?wasmInitTimeout=1` demo on real GPU (headless boot path blocked).

### Next

None — #402 DoD met. Ready for deploy verification on test.1ink.us.

---
## #387 TSL migration — 2026-08-27 — complete

### Hosts migrated (dedicated `createVfxMaterials` + `VfxNodeMaterials` twins)
EnhancedSky (verified), WeatherSystem (dead shader dedupe), SplashSystem bow-wave, PondFog, Rainbow, WaterfallSheet, WaterfallImpactZone, SunShafts (+ motes), Mist, Fireflies, CanyonDust, RockFoam, Fish/Dragonflies (`updateCritterMaterialTime`).

### Allowlist (`scripts/glsl-hosts-allowlist.json`)
- **13** construction sites: **10 dual**, **1 residual** (`PostProcessingPipeline.tsx`), **2 dormant** (`CausticsMaterial`, `EnhancedWaterMaterial`)
- Removed `createBackendShaderMaterial.ts` from allowlist; GLSL construction consolidated in `vfxDualFactory.ts`

### Checks
| Check | Status |
|-------|--------|
| `pnpm typecheck` | ✅ exit 0 — `[glsl-hosts] ok — 13 construction sites; residual 1/1` |
| `pnpm test` | ✅ 798 passed, 5 skipped |
| `pnpm test:visual-smoke` | ✅ Visual smoke passed (required gate) |
| `pnpm test:visual-smoke:tsl` | see post-install run |

### Next
Phase E native WebGPU flip remains gated on `POST_STACK_PORTED` + empty residual (post stack only).


---
## #434 B2 — P1–P4 — 2026-09-23 — complete (verified on main a3a9ce7)

**Objective:** three/@types/three → 0.185.0; node RenderPipeline post twin so no residual GLSL host remains.

**Landed:** P1 `474f2e8` (exact pins 0.185.0), P2+P3 `87e0b76` (`src/components/postProcessing/nodePostPipeline.ts` + loader + `postFrameParams.ts`; `POST_STACK_PORTED = true`; `PostProcessingPipeline.tsx` reclassified `dual`, `maxResidual: 0`), P4 docs (CLAUDE.md stack table + RENDERER.md). Note: P2 and P3 shared one commit upstream.

**Re-verification this session (no code changes):**
| Check | Result |
|---|---|
| `pnpm install` | ok |
| `pnpm typecheck` | ok — `[glsl-hosts] 13 construction sites; residual 0/0` |
| `pnpm lint` | 0 errors / 275 warnings (baseline 280, not risen) |
| `pnpm test` | 1025 passed / 5 skipped |
| `pnpm build` | ok |
| `pnpm test:visual-smoke` | passed (required gate), baselines unmoved |
| `pnpm test:visual-smoke:tsl` | passed, 0 hard page errors; glacial shot soft-skipped (SwiftShader F-8) |

Smoke needs `PUPPETEER_EXECUTABLE_PATH=/opt/pw-browsers/chromium` in this container (puppeteer's bundled Chrome is not installed).

**Baselines re-blessed:** none.
**Open risks:** `forceWebGL` still true by design; native WebGPU flip is phase C. Headless TSL smoke runs on SwiftShader, so the node-post look is only checked coarsely.
**Next step:** #434 phase C — flip `forceWebGL` behind `canEnableNativeWebgpu()`.
