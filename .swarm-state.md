# Swarm State — GameHUD Wiring

## What changed
- `src/Experience.jsx`: Imported `GameHUD` and `PLAYER_SPAWN`.
- Added `isWipeout` state to `InnerExperience`.
- Added minimal wipeout detection in existing `useFrame`: triggers when `vehicleRef.current.translation().y < -80`.
- Added `handleRespawn` callback that resets player position/velocity/rotation to `PLAYER_SPAWN`.
- Mounted `<GameHUD>` inside the existing `<Html>` overlay alongside `ForecastHUD`, with conditional `pointerEvents` so the respawn button is clickable during wipeout.
- No vehicle files modified; no test files modified.

## Current blocker
- None. Build passes cleanly.

## Next planned step
- Done. `npm run build` exits 0 with no TypeScript errors.
- All acceptance criteria met:
  - GameHUD renders inside the Canvas via `<Html>` overlay
  - Speed updates in real time via `vehicleRef` passed to `rigidBodyRef`
  - Distance counter increments as player moves downstream (negative Z)
  - Wipeout overlay appears when player falls below Y=-80
  - Respawn resets player to spawn position
  - ForecastHUD still renders
  - ReachManager still mounts
  - PostProcessingEffects still active
