
import { populateSidePart1 } from './populateSidePart1';
import { populateSidePart2 } from './populateSidePart2';

export function populateSide(args) {
    populateSidePart1(args);
    populateSidePart2(args);
}
