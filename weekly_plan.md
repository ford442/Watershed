# Watershed — Weekly Plan

## Today's focus
**2026-08-19 — User Idea mode: GitHub issue #371 (P0 Foundation) — make *navigation* honest: retire lying PLAN.md docs, namespace the 79-file `src/systems/` dump, split the 5 oversized frame-hot hosts back under ~650 lines, delete dead duals + boot-time debug leftovers.** Noah authored a fresh board **#369–#375 (08-14→08-16)** after the entire #353–#357 board landed; #371 is one of three P0 Foundations (#371 org/TS, #372 C++ toolchain, #373 rendering context) and the direct successor to #353 — *"#353 made typecheck honest; this makes navigation honest."* Confirmed against current `main` (`c5940e2`) this session, every claim in the issue verified against the tree:

- **The mechanical grind is real and bounded.** `src/PLAN.md` / `src/systems/PLAN.md` / `src/components/PLAN.md` all exist and describe a different game (systems marked "Not started" that shipped months ago; deleted duals `Player.jsx`/`RiverTrack.jsx`/`CreekCanyon.jsx` still "planned"). `src/systems/` holds **79 `*.ts` files** at the root. The 5 hosts PR #213 split under 700 lines have grown back: `RunnerPhysicsStep.ts` **852**, `FlowingWater.tsx` **783**, `BiomePalettes.ts` **781**, `TrackSegmentMeshes.tsx` **739**, `ChunkManager.ts` **712** (all verified `wc -l`). `src/systems/PostProcessing.tsx` is a dead dual re-exported only from `index.ts` (live path is `PostProcessingPipeline.tsx`). `GameHUD.tsx:276-302` runs a WASM `calculateBuoyancyAndDrag(150,0.4,0,-3)` hello-world on every boot and `console.info`s it.
- **#371 is the ideal long-horizon swarm target:** "many small PRs; mechanical" per the issue — folder moves + import rewrites (no behavior change), pure-helper host splits in the existing `TrackSegment/` pattern, doc rewrites, dead-code deletes with grep-zero assertions. Every step is independently verifiable (`pnpm typecheck` + `pnpm test` stay green; visual-smoke baselines untouched; grep counts go to zero). Exactly the tedious, save-stating, PR-sliceable work best fed to an iterative agent rather than done by hand — the same profile that made #353 a clean swarm.
- **Ride-along that folds in perfectly:** Noah's own `push fix` on 08-15 (`e0e2285`) left debug `console.log`s in `TrackManager.tsx:445` (`[TrackManager] Map binding confirmed` + `window.__watershedMapInfo`) and `MapSystem.ts:514` (`[MapSystem] ProceduralMapManager bound…`). These are exactly the boot-time console-spam class #371 Phase D targets — the swarm removes them alongside the GameHUD smoke.
- **It compounds into the whole board.** #374 (Hydrology epic) explicitly requires `systems/water/` namespace (#371 Phase B) before SWE/WASM files "have a home." #372 (C++ ABI) and #373 (context contract) both land cleaner into a de-dumped tree. Every future content PR (sixth map, new decoration) stops having to reverse-engineer *which* `PostProcessing`/`WaterForces` is live. Navigation-honest first is the unlock.

**Fix First does NOT apply** (verified this session against `origin/main` HEAD `c5940e2`): the entire **#353–#357 board landed cleanly** through PRs #358–#367 — `scripts/untyped-allowlist.json` is now `[]`, `KNOWN_LEVEL_SCHEMA_DEBT` is empty, **zero** leftover `.js/.jsx` under `src/`, `emscripten/main.cpp` split into `forces.cpp`/`swe.cpp`/`bindings.cpp` + headers, TSL Path A dual-path hosts, survival v2, ghost Phase A+B all merged. **0 open PRs.** Noah's three `push fix` direct-to-main commits (`c5940e2` deploy.py hardening, `e0e2285` a real `fallbackProgression`-consistency bugfix + debug logs, `a0e895d` WASM docs/CMake follow-ups) are hands-on debugging and hardening, **not** broken agent output. The only "cracks" (stale PLAN docs, console spam, oversized hosts) *are* the #371 scope — advancing the roadmap and doing the fix are the same act. *(Note: live `pnpm typecheck`/`test` not run — `node_modules` absent, install skipped; empty allowlist + empty schema debt + 0 `.js/.jsx` + clean PR merge history + 0 open PRs are the green signal. Flagged in Open Questions.)*

Reconciliation at startup (branch `claude/compassionate-lamport-cgfi07`, fresh from `origin/main` HEAD `c5940e2`):
- **The entire #353–#357 board (last week's focus + the whole P1 wave) LANDED.** #353 (PR #361 "Close out typecheck-surface debt: type all Environment/* hosts, retire level schema debt" + #360 honest-typecheck) → allowlist `[]`, schema debt `[]`, 0 `.js/.jsx`. #354 (PR #362 "Productize the WASM water physics path: default-on worker, quality-gated SWE, split C++" + #367 header split) → `emscripten/` is `forces.cpp`/`swe.cpp`/`bindings.cpp` + `common.h`/`forces.h`/`swe.h`. #355 (PR #363 dual-path TSL material hosts under WebGL). #356 (PR #364 survival v2). #357 (PR #365 ghost Phase A+B). Moved to Done.
- **Noah's new board (#369–#375, authored 08-14→08-16) is the active Ideas source.** Three P0 Foundations: **#371 (org/TS — navigation honest), #372 (C++ toolchain honesty — compile_commands/host tests/one force ABI/SIMD), #373 (WebGL context contract — apply quality without remounting Rapier)**. Then **#375 P1 (ghost league Phase C)**, and two P2 north-stars **#374 (Hydrology epic)** + **#369/#370 (gpu-chores / WebGPU-required probe, explicitly deferred, NOT default boot)**.
- **0 open PRs.** Clean tree, clean merge history through `c5940e2`. The long-standing SECURITY rotation item (old `DEPLOY_TOKEN` + `deploy_old.py` plaintext password in git history) remains owner-only and unchanged — still owed.
- **`recent_chats` / `conversation_search` unavailable this session** (claude.ai connectors not present in this environment). Historical context reconstructed from git history (30 commits), the 7 open GitHub issues, and `weekly_plan.md`. Flagged in Open Questions — no chat-derived "where we left off" is fabricated.

## Ideas
<!--
Write ideas here during the week as they come to you.
Routine prioritizes these over generated ideas.
Format: - [ ] Short description (optional: more context on next line indented)
Routine will mark picked items as "[in progress — YYYY-MM-DD]".
-->
- [x] Wire GameHUD into Experience.jsx — done 2026-05-06; all acceptance criteria met per .swarm-state.md
- [x] Author segments 0–12 waypoints in meander_to_waterfall.json — done 2026-05-13; PR #130 merged (deterministic decoration placement + authored waypoints)
- [x] Reactive audio validation — done 2026-05-27: all 23 MP3 stubs replaced with valid 1-second sine tones (17,180 bytes each), AudioDiagnosticsOverlay.tsx wired into Experience.jsx behind DEV gate, AudioSystem.ts updated with getLoadStatus/getActiveSounds diagnostics APIs; confirmed by .swarm-state.md
- [x] In-scene Level Editor — done 2026-05-20: PR #151 merged; LevelEditor wired via ?editor=1, useLevelEditor.ts hook + levelEditorValidator.ts added; Option A (overlay on game Canvas) selected
- [x] Runner sprint stamina HUD feedback — done 2026-06-03; PR #211 merged (Zustand stamina store, speed-reactive vignette intensity in RunnerVehicle, HUD stamina bar)
- [x] Authored environmental set-pieces — done 2026-06-24 (issue #243 closed completed): `DecorationPlacement[]` array path wired end-to-end + `rockType` override; seg-14 `slab` shelf and seg-22 three `column` pillars authored in `meander_to_waterfall.ts`. Static/collidable v1 (launch impulse + crumbling were explicitly deferred — now the two ideas below).
- [ ] [in progress — 2026-06-25] Waterfall launch-shelf v2 — make the seg-14 `slab` shelf apply a speed-gated forward+up launch impulse to the runner/raft (a new `'shelfLaunch'` tag in the existing `applyImpulseWithDebugTracking` system), serving the Sonic-speed identity. v1 shelf is visual+collidable only. Scope: full-day.
- [ ] Destructible "crumbling" slot-canyon pillars (seg-22 v2) — #243 scoped crumbling OUT: `Rock.jsx` (291 lines) has no per-instance state/health/break logic, only `boulder|slab|column` fixed `InstancedRigidBodies`. Add lightweight per-instance break-apart triggered on player proximity/contact (impulse scatter + dust VFX). Scope: multi-day (new per-instance state machine in `Rock.jsx` + physics).
- [ ] First real WGSL/NodeMaterial shader path under the existing `?renderer=webgpu` toggle — **now seeded by #251** (RiverShader + CanyonMaterial getting NodeMaterial/TSL variants) and the proposed native-WebGPU epic in the Backlog. The renderer toggle is already load-bearing (PR #219) but `createRenderer.ts:56` forces the WebGL2 backend until materials migrate. Track the remaining ~22 legacy materials here / in the epic. Scope: multi-day → multi-week.
- [x] Waterfall launch-shelf v2 — done 2026-06-25/27 (PR #250): `'shelfLaunch'` speed-gated forward+up impulse on the seg-14 slab, runner + raft wired, pure `shelfLaunch.ts` helper + 18 unit tests. Real-hardware trajectory tuning remains a playtest task, not a code task.
- [x] Renderer/material-pipeline regression guard (**#255**) — done 2026-07-08 (landed on `main`): `createRenderer.test.ts` guard constructs `RiverShader`/`CanyonMaterial`/`RiverNodeMaterial` and asserts `createGameRenderer` returns `WebGLRenderer` for both `webgl`/`webgpu`; `docs/RENDERER_CONTRACT.md` documents the invariant; `three/webgpu`+`three/tsl` Jest stub added. 182/182 pass, "guard bites" proof recorded in `.swarm-state.md`.
- [ ] [in progress — 2026-07-08] Resolve the WebGPU fork (**#256**, was "first real WGSL shader path"), path **B — retire the pretense** now that #255 guard has landed as its safety net: demote `?renderer=webgpu` to labeled-experimental (no-op is now honest, not a silent lie), delete truly-dead `.wgsl` stub files, fix README line 32's false "Custom WebGPU compute shaders (WGSL)" claim + any matching CLAUDE.md/SYSTEMS.md text, and **keep** `RiverNodeMaterial.ts`/`CanyonNodeMaterial.ts` as clearly-labeled dormant migration seeds (guard test depends on them). Path **A** (real TSL migration under `WebGPURenderer`) explicitly deferred — would loosen the WebGL-only invariant just netted. Scope: full-day (today's kimi swarm).
- [x] Reward the launch shelf: air-time / clean-launch scoring (**#257**) — **DONE: issue #257 was expanded 2026-07-01 and implemented + closed-completed 2026-07-02** (dup #259 also closed). Shipped in-code: pure `src/systems/launchScoring.ts` (tiers None/Nice/Great/Perfect + `launchScoring.test.ts`), `src/systems/LaunchScoringSession.ts` state machine (physics-step air-time, ascent debounce, session-id latch, gap-clear gate, combo delta, high-score persist), both vehicles wired (runner `terrain|airborne`, raft `water|airborne`), `GameHUD` transient popups, `cancelLaunch()` on void-fall wipeout. **Correction to last week's plan, which wrongly listed this as "not yet built."**
  - Follow-up (this week's Dispatch-B): the **stateful `LaunchScoringSession` machine has no unit tests** — only the pure fn is covered (#257's bar). New issue filed to test-harden it (Kimi's review flagged exactly this). Decoupled from kimi's renderer/docs work — touches `src/systems/` only.
- [ ] Destructible "crumbling" slot-canyon pillars (seg-22 v2) — #243 scoped crumbling OUT: `Rock.jsx` (291 lines) has no per-instance state/health/break logic, only `boulder|slab|column` fixed `InstancedRigidBodies`. Add lightweight per-instance break-apart triggered on player proximity/contact (impulse scatter + dust VFX). Scope: multi-day (new per-instance state machine in `Rock.jsx` + physics). **Parked.**
- [x] Resolve the WebGPU fork (**#256**), path **B — retire the pretense** — done 2026-07-10 (commit `0d1ff36`): README false "Custom WebGPU compute shaders (WGSL)" claim replaced with honest experimental framing; `heightmap_flow.wgsl` labeled DORMANT/EXPERIMENTAL; `RiverNodeMaterial`/`CanyonNodeMaterial` kept as labeled dormant seeds; `RENDERER_CONTRACT.md` updated. Path A (TSL migration) still deferred.
- [x] Reward the launch shelf: air-time / clean-launch scoring (**#257**) — done 2026-07-03 (commit `6b11d85`): `launchScoring.ts` + `LaunchScoringSession.ts` + tests, wired into both vehicles + `ScoreSystem.ts`, HUD launch/air-reward popups. Tracked as open GitHub issue **#273** — needs real-hardware playtest + issue close only.
- [x] Destructible "crumbling" slot-canyon pillars (seg-22 v2) — promoted to GitHub issue **#274** (open, multi-day, parked). Lives on the board now, not here.

> **NOTE (2026-08-05):** The Ideas source is now **Noah's fresh GitHub board #336–#340 + #328 (authored 08-03)**. The prior #312–#316 board largely **executed** (Hydro-Dam #325, physics-worker water forces #324, speed-wind audio, survival Track A, launch-hour/portage, settings wiring) — moved to Done/reconciliation. Ideas-mode picks now come off the **#336–#340** board. Today's pick: **#337 (P0 Foundation — adaptive WebGL quality contract), marked in progress below.** #336's high-value slice already landed via the swarm; its remainder is an incremental grind (partly today's decoupled Copilot unit).
- [x] **#271 — Wire `MapSystem.ts` into `TrackManager`** — DONE (landed on `main`: `TrackManager.jsx → .tsx`, `MapSystem` drives the treadmill, `MapSystem.parity.test.ts` + `MAPSYSTEM_STEP5_CONTRACT.md`). Unblocked the biome roadmap as intended.
- [x] **#313 (P0 Foundation) — DONE (quick wins landed; residual → #336).** pnpm/Corepack hygiene (`packageManager: "pnpm@10.33.0"` + AGENTS.md note) ✅, registry contract tests (`registry.contract.test.ts`/`registry.contract.ts`) ✅, MapSystem type/gen split (#321, `6ec8c92`) ✅. Residual typed surface (part c) correctly became **#336**; its frame-hot slice (materials→.ts, RiverShader→.ts, FlowingWater/EnhancedSky `.d.ts` facades) already landed via the swarm.
- [x] **#315 (P0 Performance) — DONE.** ADR hybrid shipped: C++ water forces co-located with Rapier in one physics worker (#324, `f735b59` "feat(physics): co-locate Rapier and WASM water forces in worker"), feature-flagged with TS fallback. Now continued/productized by new **#338** (default-on worker + live SWE mesh).
- [x] **#312 (P1 Content) — DONE.** Hydro-Dam biome landed (#325, `05dc46a` "feat(maps): add Hydro-Dam biome with vortex drain and campaign wiring").
- [x] **#314 (P1 Feature) — DONE.** FlowForecast agency landed: launch-hour selection + portage cache survival (`069de1f`) + survival Track A MVP (wetness/loadout/checkpoints, `9eda3a9`).
- [ ] **#316 (P2 Epic) — survival depth + TSL material path A.** Survival Track A MVP landed (wetness/loadout/checkpoints); TSL/NodeMaterial path A still deferred (dormant seeds retained). North-star; does not gate P0/P1.

### New board — authored 2026-08-03 (active Ideas source)
- [ ] **#336 (P0 Foundation) — make typecheck honest: migrate residual JSX/JS + stop map cast drift.** Frame-hot slice already landed (materials/RiverShader → `.ts`, FlowingWater/EnhancedSky `.d.ts` facades). **Remainder:** 34 `Environment/*.jsx` + 6 pure `.js` (`ObstaclePool.js`, `utils/{Rock,Tree,Vegetation}Shader.js`, `Environment/{DebrisAssets,TreeAssets}.js`) + registry ajv load-assert (blocked: shipped map JSONs violate `level.schema.json` today — see `KNOWN_SCHEMA_DEBT`) + shrink `tsconfig.typecheck.json` exclusions. Incremental / multi-PR. **The 6-.js slice is today's decoupled Copilot unit (Dispatch B).**
- [ ] **[in progress — 2026-08-05] #337 (P0 Foundation) — adaptive WebGL quality contract.** Pure `deriveRendererContextOptions(quality, settings)` in `src/rendering/`; wire DPR/antialias/shadow-map size to quality presets on Canvas (remount via `key` on quality change, same pattern as renderer preference); set `outputColorSpace = SRGBColorSpace` + `toneMapping = ACESFilmicToneMapping` once in `createGameRenderer`; `webglcontextlost`/restore recovery; unit tests for the derive fn; `docs/reference/RENDERER.md`. WebGL-only invariant preserved. **Today's kimi-cli swarm.**
- [ ] **#338 (P1 Performance) — default-on Rapier+WASM worker + live SWE heightfield on the water mesh.** Continues #315: flip worker default-on with `?physicsWorker=0` kill switch + shadow-mode parity, then couple `stepShallowWater` → `DataTexture` → `FlowingWater` vertex displacement (quality-gated). Depends on #337 quality scaling. Scope: L–XL phased.
- [ ] **#339 (P1 Architecture) — multi-Reach seamless handoff + continuous journey mode.** Act on transition thresholds (prefetch + control-point-continuous append + biome cross-fade, no Canvas remount); StartMenu Single-map vs Journey; continuity helper + tests. Scope: L.
- [ ] **#340 (P1 Content) — Delta climax map** (sunset raft finale, wide 80–120 channel, braided washouts via FlowForecast states, beach finish → journey results). Replaces the `delta_rapids` stub. Depends on #337 (wide-water scaling) + prefers #339 handoff. Scope: L.
- [ ] **#328 (Refactor) — split `src/style.css` (~1450 lines) into modules < 700 lines each**, keep a thin barrel so imports stay stable. **DONE — landed `6318bc3` / PR #343** (`src/styles/` per-section modules). Moved to Done.

### New board — authored 2026-08-12 (EXECUTED — moved to Done 2026-08-19)
> **NOTE (2026-08-19):** The **entire #353–#357 board landed** (PRs #358–#367; verified against `main` `c5940e2` — allowlist `[]`, schema debt `[]`, 0 `.js/.jsx`, `emscripten/` split, TSL dual-path, survival v2, ghost A+B). No longer a live source. See Done.
- [x] **#353 (P0 Foundation) — finish residual typecheck surface + retire level-schema debt.** DONE (PR #361 + #360). `scripts/untyped-allowlist.json` = `[]`; `KNOWN_LEVEL_SCHEMA_DEBT` = `[]`; zero `.js/.jsx` under `src/`.
- [x] **#354 (P1 Performance/C++) — default-on worker + quality-gated SWE + split C++.** DONE (PR #362 + #367). `emscripten/main.cpp` → `forces.cpp`/`swe.cpp`/`bindings.cpp` + `common.h`/`forces.h`/`swe.h`; worker default-on; `getVersion() = 4`.
- [x] **#355 (P2 Epic) — TSL Path A.** DONE (PR #363) — `?material=tsl` dual-path water/river/canyon hosts under WebGLRenderer. Visual-parity/gating follow-ups tracked by #373/#370.
- [x] **#356 (P1 Gameplay) — Survival v2.** DONE (PR #364) — spatial portage/caches, slipperiness→friction, raft paddle coupling.
- [x] **#357 (P1 Feature) — Ghost time-trial league.** DONE Phase A+B (PR #365) — PB storage, `.wsghost` export/import, HUD time-delta. **Phase C (rival race + splits + results screen) re-scoped as open #375.**

### New board — authored 2026-08-14→08-16 (active Ideas source)
> **NOTE (2026-08-19):** The Ideas source is now **Noah's fresh GitHub board #369–#375**. The prior #353–#357 board fully executed (see above). Three P0 Foundations lead it. Today's pick: **#371 (org/TS — navigation honest), marked in progress below** — the mechanical, PR-sliceable successor to #353 and the ideal swarm target.
- [ ] **[in progress — 2026-08-19] #371 (P0 Foundation) — retire stale plans, namespace `systems/`, split 800-LOC hosts, delete dead duals.** Phase A: delete/archive lying `src/*/PLAN.md`, rewrite README roadmap, fix `CODE_HEALTH_GUIDE.md` mount diagram. Phase B: namespace the 79-file `src/systems/` into `journey/survival/ghost/water/reach/score/audio/` subfolders (import rewrites, no behavior change), CI-ban new root-level `systems/*.ts`. Phase C: split the 5 hosts (`RunnerPhysicsStep.ts` 852, `FlowingWater.tsx` 783, `BiomePalettes.ts` 781, `TrackSegmentMeshes.tsx` 739, `ChunkManager.ts` 712) under ~650 lines in the `TrackSegment/` pattern. Phase D: delete `systems/PostProcessing.tsx` dead dual, remove GameHUD boot-time WASM smoke `console.info`, **+ ride-along: strip Noah's 08-15 debug `console.log`s** (`TrackManager.tsx:445` / `MapSystem.ts:514`). `pnpm typecheck`+`test` stay green; visual-smoke baselines untouched. **Today's kimi-cli swarm.**
- [ ] **#372 (P0 Foundation) — C++ toolchain honesty.** Phase A: `CMAKE_EXPORT_COMPILE_COMMANDS ON` + `compile_commands.json` for clangd; make `forces.cpp`/`swe.cpp` host-compilable (quarantine `emscripten.h` to `bindings.cpp`, portable KEEPALIVE macro), link a host smoke binary. Phase B: collapse to **one** water-force ABI (stop `raftPhysicsRuntime` double-applying `physics/WaterForces`). Phase C: real `wasm_simd128` SWE kernel or drop `-msimd128` from the story. **Phase A is today's decoupled Copilot unit (Dispatch B) — pure `emscripten/*` + `CMakeLists.txt` + `docs/reference/WASM.md`, zero overlap with #371.** Scope: L phased.
- [ ] **#373 (P0 Foundation) — complete the WebGL context contract: apply quality without remounting Rapier.** Phase A: pin missing `gl` attributes (`alpha:false`, `depth/stencil`, `failIfMajorPerformanceCaveat` on medium+ with CI opt-out), derive tests. Phase B: split the Canvas `key` so quality changes apply live (`dpr`/shadowMap/SWE budget) instead of tearing down Physics+treadmill+WASM. Phase C: LevelEditor Canvas uses the same derive helper. Scope: M–L.
- [ ] **#375 (P1 Feature) — Ghost league Phase C:** checkpoint splits (pure TS), a results screen that teaches the line (DOM, not in-canvas), PB+one-rival slot (cyan/amber, cap 2 bodies). Codec version bump w/ v1 back-compat. Offline-first, no new deps. Soft-depends #372/#373 for cross-machine fairness. Scope: L.
- [ ] **#374 (P2 Epic) — Hydrology as identity:** nonlinear SWE (wetting/drying, Rusanov/HLL) + canyon bathymetry as bed `b` + authored `hydroEvents[]` (inflowPulse/drop/vortex/braid). North-star; blocked on #372 (one ABI) + #373 (no-remount) + #371 (`systems/water/` home). Does NOT gate P0/P1. Scope: XL multi-week.
- [ ] **#369 (P2) — gpu-chores** (blur/hist/reduce for HUD/minimap/thumbs; SWE stays domain) and **#370 (probe) — WebGPU-required hard-fail boot.** Both explicitly deferred: #370 must NOT become the default boot while production materials are GLSL and post is WebGL-only; #369 must not own SWE. Research/later-wave, do not gate content.

## Backlog
<!--
Unfinished items, known bugs, deferred ideas.
Routine maintains this automatically — you can add items too.
-->
- [ ] **SECURITY — code-side leak CLOSED 2026-06-25 (commit `5b07a06`); ROTATION still pending (owner action).** `deploy.py` and `deploy_old.py` no longer hard-code secrets — both read `DEPLOY_TOKEN` / `DEPLOY_SFTP_PASSWORD` from the environment and fail loudly (non-zero exit, clear message) when unset; verified. **BUT** the old token (`6de44dca…`) and SFTP password (`GoogleBez12!`) are still in git history and must be treated as compromised: **rotate both on the VPS now**, and decide whether to scrub git history. Those two steps are the only remaining work and they're owner-only — this routine can't rotate credentials.
- [ ] **Navigation-honesty debt → now the core of the picked #371 (in progress 2026-08-19).** Verified against `main` `c5940e2`: `src/PLAN.md`/`src/systems/PLAN.md`/`src/components/PLAN.md` all describe a different game; 79 `src/systems/*.ts` at root (undated dump); 5 hosts back over 700 lines (852/783/781/739/712); `systems/PostProcessing.tsx` dead dual; `GameHUD.tsx:276-302` boot-time WASM hello-world `console.info`; Noah's 08-15 debug `console.log`s in `TrackManager.tsx:445`/`MapSystem.ts:514`. All in-scope for #371 — not a standalone backlog item, it's today's focus.
- [ ] **Issue #251 (filed + expanded 2026-06-25, Dispatch B)** — three things in dependency order: (a) add a headless Jest semantic+matrix-truth guard for the RiverShader moss world-normal (verified *already correct* — Grok's `smoothstep(2,4)`-always-zero alarm was a false positive conflating the meters-based height band `:281` with the normal mask `:285`); (b) fix the **real** audit find — `CanyonMaterial.js:135` view-space `vNormal` consumed as world-space at `:350` (rim/heat-glow) and `:261` (parallax) — **owner approved the wall-appearance change, lands in #251's PR**; (c) migrate RiverShader + CanyonMaterial off `onBeforeCompile`/`ShaderMaterial` to `NodeMaterial`/TSL so they run under native WebGPU (GLSL kept as WebGL fallback). Guard extends the **existing** `RiverShader.test.ts` (it already tests USE_COLOR + fallback, not the normal wiring).
- [ ] **Native WebGPU epic (proposed in #251 OQ4)** — `createRenderer.ts:54-63` already lazy-loads `WebGPURenderer` but pins the **WebGL2 backend** (`:56`: "Force the WebGL2 backend until materials migrate to NodeMaterial") because native WebGPU rejects all the legacy materials. The flip is all-or-nothing: ~7 `onBeforeCompile` shaders + ~17 `ShaderMaterial` components must all migrate first. #251 does 2 of them (RiverShader, CanyonMaterial); the remaining ~22 + the `:56` flip want their own tracking epic. Awaiting Noah's yes on OQ4 before filing it.
- [x] **RESOLVED 2026-07-29 — blocking typecheck gate now in CI.** `build.yml:15` has a `typecheck` job running `pnpm typecheck` (last week's kimi swarm target landed). Moved to Done. The *residual* holes that gate can't see (registry contract, JSON-shape assert, 52 untyped `.js/.jsx` files) are today's #313 focus, tracked in Ideas — not a Backlog item anymore.
- [x] **GitHub issue board #267–#278 — FULLY EXECUTED, 0 open issues.** All twelve landed (see Done). No longer a live source; the biome roadmap + this-week's-landings are now the idea surface. The old `webgpu-react-app` package name (#269) and react-scripts/`import.meta` caveat (#270) are resolved — `name: watershed`, runner is Vitest.
- [→] **Registry/campaign type drift → now the core of the picked #313 (in progress 2026-07-29).** `MAP_REGISTRY` keys, `MapRegistryId`, `resolveMapRegistryId`, and `listMapsForMenu` metadata must stay in lockstep; the lumber entry drifted two weeks ago and only a red `tsc` caught it. #313's contract tests close this — moved from "note" to active work. Not a standalone backlog item anymore.
- [ ] **CLAUDE.md doc drift (covered by #278):** line 28 claims "dead WGSL stubs have been removed" but `public/shaders/heightmap_flow.wgsl` still exists (correctly relabeled DORMANT/EXPERIMENTAL and fetched by `HeightmapFlow.ts`, not removed); directory map duplicates `hooks/`/`configs/`/`constants/`/`maps/`/`materials/`; version says "three 0.160" while lockfile resolves 0.168. #278 is the cleanup.
- [ ] **SECURITY — CODE-SIDE FIXED (PR #260, 2026-07-08); SECRET ROTATION STILL OWED BY NOAH (20+ days).** `deploy.py` now reads `DEPLOY_TOKEN` from `os.environ.get(...)` and fails loud if unset — the hardcoded token is out of the *current* file. **But the old token (`6de44dca…cadc0`) and the `deploy_old.py` plaintext SFTP password (`'GoogleBez12!'`, `ford442@1ink.us`) remain in git history and must be treated as compromised.** Owner action, unchanged and now urgent: **rotate both credentials now**, and decide whether to scrub history (BFG/filter-repo). No code task left here — this is 100% on Noah. **Top of the list — only item with real blast radius.**
- [ ] **Test-env note:** `createRenderer.test.ts` requires the native `canvas` npm package to give jsdom a GL context; `WatershedWasm.test.ts` needs `import.meta` transform support. Both fail in constrained/network-restricted installs (`--ignore-scripts`, blocked proxy) but are expected green in real CI. If GitHub CI ever shows these red, that's a real config regression — worth a one-line note in `docs/TESTING.md`.
- [ ] Verify RiverShader.js moss effect receives correct world normals from TrackSegment.jsx terrain mesh — carried 4+ weeks; still no visual confirm. **NOT a Dispatch unit today** — it touches `RiverShader.js`/`TrackSegment/`, which collides with today's kimi renderer/material audit. Re-assign to a Copilot unit next week once the #256 cleanup is merged. Decoupled work only.
- [ ] CLAUDE.md "Known split" warning re-check — SYSTEMS.md's BiomeSystem.tsx contract card documents the EnhancedSky/BiomeProvider legacy-prop split as "Known Pain." The 2026-06-24 F-10 work included a "Unify biome state: BiomeProvider as single source of truth" commit (`23f17ed`) — **re-verify whether that card is now stale** and update it if so.
- [ ] Issue #240 — Live browser smoke test + repeatable full first-map run: was open as of last run; its blockers (F-2/F-3/F-4, then F-9) are now all fixed, and `verification/` now holds per-section captures (`firstmap-glacier/slot/waterfall/splash/delta-webgl.png`) suggesting a full headless journey run was captured. **Re-confirm #240 is actually closeable** (or close it) — GitHub currently shows 0 open issues, so it may already be closed.
- [ ] Set-piece v1 verification gap — issue #243's acceptance criteria included confirming each set-piece renders *only* at its target segment via `teleportToSegment(14)`/`(22)`, but SwiftShader-headless capture has documented parity caveats. Worth one real-hardware visual confirm that the seg-14 slab and seg-22 columns actually appear and don't leak into neighbors.

## Done
<!--
Completed items, routine archives here with date.
Prune occasionally when this gets long.
-->
- [x] 2026-08-19 — **Reconciliation: the ENTIRE #353–#357 board (last week's focus + the whole P1 wave) LANDED.** Verified against `origin/main` HEAD `c5940e2`, not just plan notes. **#353** (PR #361 "Close out typecheck-surface debt: type all Environment/* hosts, retire level schema debt" + #360 honest-typecheck merge) — `scripts/untyped-allowlist.json` is now `[]`, `KNOWN_LEVEL_SCHEMA_DEBT` is `[]`, **zero** `.js/.jsx` under `src/`. **#354** (PR #362 "Productize the WASM water physics path: default-on worker, quality-gated SWE, split C++" + #367 header split) — `emscripten/main.cpp` → `forces.cpp`/`swe.cpp`/`bindings.cpp` + `common.h`/`forces.h`/`swe.h`, worker default-on. **#355** (PR #363 dual-path TSL material hosts, `?material=tsl`). **#356** (PR #364 survival v2 — portage/caches, slipperiness friction, raft paddle coupling). **#357** (PR #365 ghost Phase A+B — PB storage, `.wsghost` export/import, HUD time-delta; Phase C re-scoped as open #375). Foundation green → not Fix First.
- [x] 2026-08-19 — **Reconciliation: three `push fix` direct-to-main commits by Noah** — `c5940e2` (08-17) deploy.py hardening (+43 lines); `e0e2285` (08-15) a real `fallbackProgression`-consistency bugfix in `MapSystem.ts` (`JSONMapManager`/`ProceduralMapManager` now share the same fallback table) + debug `console.log`s; `a0e895d` (08-13) WASM docs/CMake/header follow-ups. Hands-on debugging + hardening, not broken agent output. The debug logs are folded into today's #371 Phase D ride-along.
- [x] 2026-08-12 — **Reconciliation: last week's focus #337 (P0 Foundation — adaptive WebGL quality contract) LANDED.** Merged via PR #352 (`6376a85`); commits `a1ed039 feat(rendering): adaptive WebGL quality contract (#337)` + `a6988eb fix(rendering): wire shadow maps to quality contract (#337)` + `4008c56`/`ad5c716` (DPR+shadow presets). The DPR/AA/shadow-preset wiring + `ACESFilmicToneMapping`/`SRGBColorSpace` + `webglcontextlost`/restore recovery all landed in `src/rendering/`. Independently certified by the fresh #355 issue body: *"Adaptive quality context (#337) is solid: DPR/AA/shadows/ACES/SRGB, context-loss recovery; `logarithmicDepthBuffer` intentionally off."* Foundation green → not Fix First.
- [x] 2026-08-12 — **Reconciliation: the #336–#340 + #328 board fully executed / closed (0 open issues on it).** GitHub shows only the fresh #353–#357 open. #337 quality contract merged (#352); #339 multi-Reach seamless handoff landed (`48f1f92 feat(journey): seamless multi-map/reach handoff without Canvas remount`, #347); #340 delta climax → sunset raft finale landed (`e30fa75`, #348 + `8b46f11` journey-results test); #328 style.css split into `src/styles/` modules landed (`6318bc3`, #343). #336's residual typed-surface work re-scoped by Noah as the new **#353**. Verified against current `main` (`6376a85`), not just plan notes.
- [x] 2026-08-12 — **Reconciliation: adjacent landings since 08-05** — `#349` DebrisAssets.js/TreeAssets.js → TypeScript (`2c42e6a`); `#350`/`#351` boot-loader readiness + start-menu feedback (`34ffa69`); visual-smoke baseline + markdown-path fixes for renamed `.jsx` (`09057a6`/`0f43662`); `9a629ca ci: remove pnpm version from action-setup`. Clean tree, 0 open PRs.
- [x] 2026-08-05 — **Reconciliation: #313 (P0 Foundation) landed its quick wins; residual spun into #336.** Verified against `origin/main` HEAD `2e7afa4`: `package.json` carries `packageManager: "pnpm@10.33.0"`; AGENTS.md documents pnpm/`pnpm-lock.yaml`; `src/maps/registry.contract.test.ts` + `registry.contract.ts` present; MapSystem type/gen split landed (#321, `6ec8c92`). The frame-hot slice of the residual typing also landed (per `.swarm-state.md` + tree: `src/materials/*` all `.ts`, `src/utils/RiverShader.ts`, `FlowingWater.d.ts`/`EnhancedSky.d.ts` facades). Foundation green → not Fix First.
- [x] 2026-08-05 — **Reconciliation: the #312–#316 board largely executed since 07-29.** #315 physics-worker C++ water forces (#324, `f735b59`); #312 Hydro-Dam biome (#325, `05dc46a`); #314 launch-hour selection + portage cache (`069de1f`) + survival Track A MVP wetness/loadout/checkpoints (`9eda3a9`); speed-based wind audio bed (`123d4a2`); settings-store wiring to KeyboardControls + post-processing (#319-era). Latest merges #324–#327 all clean; #327 (`2e7db10`) is itself a landed bug fix — runner ground-ray excludes self + starts rays above the capsule. Verified against current `main`, not just plan notes.
- [x] 2026-08-05 — **Reconciliation: stale PR #304 finally merged (`2e7afa4`).** Carried the deploy-credential env fix (already in via #260) + a 6-line weekly_plan edit. Post-merge check: `deploy.py` still reads `DEPLOY_TOKEN` from `os.environ.get(...)` and fails loud if unset — no hardcoded secret re-introduced. No open PRs remain on the repo.
- [x] 2026-07-29 — **Reconciliation: last week's Fix First fully landed on `main`.** The blocking typecheck CI gate (last week's kimi-cli swarm root-cause target) is live — `.github/workflows/build.yml:15` `typecheck` job runs `pnpm typecheck`. The 3-line symptom fix merged as **#300**. Verified this session against `origin/main` HEAD `1a15754`: `tsc -p tsconfig.typecheck.json --noEmit` exits 0; `typecheck` script + `tsconfig.typecheck.json` in place. Foundation green → not eligible for Fix First.
- [x] 2026-07-29 — **Reconciliation: new landings since 07-22** — **#302** (Cursor: fix load lockup from spawn/init update storm), **#303** (persistent player Settings panel — audio/controls/graphics; the #301 work landed), **#305**, and PR **#299**'s visual-smoke CI job (`test:visual-smoke` script + `verification/visual_smoke.mjs` present). Verified against current `main`, not just plan notes.
- [x] 2026-07-22 — **Fix First LANDED this session: un-red `tsc` on `main`.** Fixed 3 type errors introduced by this week's Cursor-agent merges — `MapRegistryId` union missing `lumber` (`registry.ts`, from Lumber Flume #296), and a `readonly`→mutable dam-release cast (`InnerExperience.tsx:96`, from FlowForecast #298). Resolved the consequent `campaign.test.ts` failure by adding `MapDefinition.menuHidden` and filtering `lumber` (a "Map 0.5" deep-link-only interstitial) out of `listMapsForMenu()`. Verified: `tsc -p tsconfig.typecheck.json --noEmit` exit 0; `vitest run` **356 passed / 2 skipped**. Root cause (no blocking typecheck CI gate) handed to today's kimi-cli swarm.
- [x] 2026-07-22 — **Reconciliation: the entire #267–#278 board LANDED and closed (0 open issues).** #267 tsc-config, #268 screenshot purge, #269 TS5 + `name: watershed`, #270 Vitest migration, #271 MapSystem→TrackManager (`TrackManager.tsx` + parity test + Step-5 contract doc), #272 TS scene migration, #273 air-time scoring, #274 destructible pillars (`PillarFragmentRegistry.ts`), #275 WASM SWE sim, #276 persistence + ghost replay (`<GhostReplayer />`), #277 Glacial Melt (`glacial_source`), #278 docs hygiene (#295). Verified against `origin/main` HEAD `42e608d`, not just plan notes.
- [x] 2026-07-22 — **Reconciliation: new feature work beyond the board also landed** — multi-map campaign UX + persistence (`campaign.ts`, StartMenu, #297), Lumber Flume biome (#296), FlowForecast→segment-state gameplay loop (#298), TrackSegment refactor passes 1–3 (#291–#293), separate Rapier collision mesh (#294). Confirmed last week's User-Idea focus (#271) reached `main`.
- [x] 2026-07-15 — Reconciliation confirmed **last week's focus #256 path B (retire the WebGPU pretense) LANDED** on `origin/main` (commit `0d1ff36`, 2026-07-10). README line 32's false "Custom WebGPU compute shaders (WGSL)" claim replaced with honest experimental framing referencing `HeightmapFlow.ts`; `public/shaders/heightmap_flow.wgsl` given a "DORMANT/EXPERIMENTAL — not wired into the live renderer" header; `RiverNodeMaterial.ts`/`CanyonNodeMaterial.ts` kept as labeled dormant migration seeds (the #255 guard depends on them); `docs/RENDERER_CONTRACT.md` updated. Path A (TSL migration) still deferred. Verified directly against current-branch files, not just plan notes.
- [x] 2026-07-15 — Reconciliation **correction: air-time / clean-launch scoring (#257) actually LANDED 2026-07-03** (commit `6b11d85`), not "carried/unbuilt" as the 2026-07-08 plan assumed. `src/systems/launchScoring.ts` + `LaunchScoringSession.ts` (+ their tests) wired into `RunnerVehicle/hooks/RunnerPhysicsStep.ts`, `RaftVehicle/hooks/useRaftControls.ts`, and `ScoreSystem.ts`; `GameHUD.tsx` shows launch-popup + air-reward-popup via `launchPopup`/`latestReward` store subscriptions; `GameState.ts` carries both fields. Now tracked as open GitHub issue **#273** — code complete, only a real-hardware playtest + issue close remain.
- [x] 2026-07-08 — **#255 renderer/material regression guard (last week's Fix First) LANDED & MERGED on `main`.** `src/rendering/createRenderer.test.ts` extended to construct `RiverShader`/`CanyonMaterial`/`RiverNodeMaterial` and assert `createGameRenderer` returns `THREE.WebGLRenderer` for both `webgl` and `webgpu` preferences (+ legacy GLSL materials prepare without NodeMaterial-incompat errors); `docs/RENDERER_CONTRACT.md` created (invariant + material/renderer compatibility matrix + the single rule for future WebGPU migration); `src/rendering/__mocks__/threeWebgpu.ts` + `jest.moduleNameMapper` stub the ESM-only `three/webgpu`/`three/tsl` subpaths. `.swarm-state.md` records a "guard bites" proof (temporarily returning a fake WebGPURenderer → 4 guard tests fail; reverted → green). 182/182 pass locally minus the known `WatershedWasm.test.ts` `import.meta` env quirk. Verified directly against current `main` code.
- [x] 2026-07-08 — **Deploy credential hardening (last week's Dispatch E) LANDED & MERGED (PR #260).** `deploy.py` reads `DEPLOY_TOKEN` from `os.environ.get(...)`, fails loud if unset, `--dry-run` flag added. Code-side of the SECURITY backlog item closed; secret rotation remains Noah's (git history still carries the old secrets — see Backlog).
- [x] 2026-07-08 — **PR #254 pointer-lock `WrongDocumentError` fix MERGED** (`504026e` → `8620b76`). `App.tsx` `requestPointerLockSafely` guards against stale-canvas / already-locked state. The draft that was awaiting Noah's browser confirm is in.
- [x] 2026-07-01 — **Launch-shelf v2 (last week's focus) confirmed landed & merged (PR #250)** against current code: `shelfLaunch` tunables (`vehicleTuning.ts`), seg-14 `launchShelf` marker, `LaunchShelfConfig` in `MapSystem.ts`, pure `src/vehicles/utils/shelfLaunch.ts` helper, runner + raft wired via `applyImpulseWithDebugTracking('shelfLaunch', …)`, 18 unit tests (`shelfLaunch.test.ts`) passing here. Real-hardware arc tuning stays a playtest task (documented in `.swarm-state.md`).
- [x] 2026-07-01 — **Post-launch renderer turbulence resolved (PRs #252, #253).** A WebGPU `NodeMaterial` experiment broke the production WebGL path (`"c is not a constructor"` / `reading 'replace'`); #252 made `createGameRenderer` return `WebGLRenderer` for both preferences, #253 restored the GLSL `RiverShader.js`/`ShaderMaterial` `CanyonMaterial.js` and fixed `watershed_native.js` BASE_URL. Both merged; local material suites (RiverShader/FlowingWater/MapSystem/shelfLaunch) green. **Follow-up gap → today's Fix First (#255 regression guard).**
- [x] 2026-06-25 — Reconciliation run confirmed the whole 2026-06-24 board landed and merged. **F-9** fixed (ChunkManager init-race → `pendingSynthesizesRef` queue flushed after `initializePool` in `TrackManager.jsx`; `docs/TESTING.md` marks Fixed). **F-10** fixed (dedicated `slotCanyon`/`glacier` palettes in `BiomePalettes.ts`, `BIOME_ID_MAP` collapse removed, `GameHUD` `SLOT CANYON`/`GLACIER` labels). **PR #247** (debris regression) merged. **PR #249** (deploy-pipeline dead-code removal) merged. **Issue #243 "Authored environmental set-pieces" completed & closed** — full `DecorationPlacement[]` plumbing wired (`MapSystem`→`ChunkManager`→`usePlacementData` with `rockTypeOverride`) + seg-14 `slab` shelf + seg-22 ×3 `column` pillars authored. Tests green (`CI=true npm test` exit 0). Verified all of the above directly against current-branch code, not just trusting the prior plan's notes.
- [x] 2026-06-25 — Last week's Dispatch B shipped: SYSTEMS.md now has contract cards for `WaterReflection.jsx` (lines 340–388) and `WaterInteraction.jsx` (line 397+) — the last two live-wired systems that lacked them. That backlog gap is closed.
- [x] 2026-06-24 — Found and fixed a live, fully-blocking regression on `main` (commit `bd46c3e`): `ReferenceError: debris is not defined` on every active `TrackSegment` render, caused by a direct push that deleted the `debris` feature but left 4 dangling references in `usePlacementData.js`/`populateZSteps.js`/`populateSidePart1.js`. Fixed (5 lines removed across 3 files), verified via build + headless WebGL capture (`pageErrors: []`, `webglVerified: true`). Merged via PR #247.
- [x] 2026-06-24 — Confirmed last week's F-2/F-3/F-4 Fix First (`teleportToSegment` → synthetic `segment-enter` replay, `WaterFlowForces` shape coercion, `ReactiveAudio`/`useSegmentAudio`/`useCameraShake` non-finite guards) is genuinely fixed and clean per `docs/TESTING.md` + `.swarm-state.md` (179/179 tests, clean capture report) — no rework needed.
- [x] 2026-06-24 — Re-verified issue #241 directly against current code (not just trusted the backlog note): DebugPanel clean-test gating and FlowingWater fallback-warning spam are both already resolved. Recommending the issue be closed.
- [x] 2026-06-15/16 — Smoke-test infra for issues #240/#241 (partial, in progress): `cleanTestMode.ts` (+ test) for `?cleanTest=1`/`?screenshot=1` overlay hiding; `resetRunSession.ts` to stop audio/weather/biome sticking in end-of-map state on journey-loop restart; `verification/webgl_screenshots.mjs` (Puppeteer) + `verification/webgl_capture.py` (Playwright, topdown workaround for SwiftShader sky-only frames) capture harnesses; `docs/TESTING.md` "Known issues / friction log" (F-1–F-7) documenting exactly what's still broken. Surfaced real bugs (F-2/F-3/F-4) — see Backlog, today's Fix First.
- [x] 2026-06-11 — WASM build hardening completed (PRs #223, #224): graceful `emcc` skip restored in `build.sh`; pthread worker check updated for modern Emscripten's embedded-worker mode. Combined with the CI `build-without-emscripten` job already in `.github/workflows/build.yml` and the portable `emsdk_env.sh` discovery already in `build_colab.sh`, last week's WASM hardening backlog item is now fully resolved.
- [x] 2026-06-10/11 — PR #212 merged: fixed `mergeCompatibleGeometries` crash on mismatched geometry attributes across 6 Environment components (TreeAssets, DebrisAssets, Ferns, Reeds, Wildflowers, Dragonflies) that was causing ErrorBoundary trips / WebGL context loss.
- [x] 2026-06-10/11 — PR #221: fixed `isSlotCanyon is not defined` crash in `TrackSegment/` introduced by the prior week's large-file refactor (PR #213).
- [x] 2026-06-10/11 — PR #219: toggleable WebGPU/WebGL2 renderer with debug helpers (`?renderer=webgl` query param now load-bearing for the smoke-test harness above). PR #218: docs accuracy pass on markdown validator. PR #220: Cursor Cloud dev environment setup notes in AGENTS.md.
- [x] 2026-06-10 — Hotfix: restored graceful WASM build skip in `emscripten/build.sh` — a 2026-06-06 regression (commit b63abac) had reordered `source /root/emsdk/emsdk_env.sh` before the `command -v emcc` check under `set -euo pipefail`, causing `npm run build` to exit 1 on any host without Emscripten at that exact path. Restored portable emsdk_env.sh discovery (5 candidate paths) ahead of the graceful skip-and-exit-0 check; verified both `build.sh` and `build.sh --threads` exit 0 cleanly (commit 5496c14).
- [x] 2026-06-06/07 — PR #213 merged (Jules): refactored `TrackSegment.jsx`, `RunnerVehicle.tsx`, `RaftVehicle.tsx` (all >1000 lines) into smaller modules under 700 lines — new `TrackSegment/` (index.jsx, TrackSegmentMeshes.jsx, PondFog.jsx, hooks/), `RunnerVehicle/` and `RaftVehicle/` subdirectories with constants/audio/physics hooks split out; also added pond fog VFX, raft paddle/splash/collision audio, runner footstep/jump/landing audio, and camera shake.
- [x] 2026-06-03/04 — Delta biome authored as game conclusion (5 deliverables, .swarm-state.md, 147/147 tests pass): `TrackBiomeId` extended with `'delta'` + `TRACK_BIOMES.delta` profile; segments 31–38 authored in `meander_to_waterfall.ts` (Delta Approach → Delta Entry → Wide Delta → Final Stretch → segment 38 `journeyComplete: true`); `GameState.ts` gained `isJourneyComplete`/`setJourneyComplete` (finalizes high score on completion); `TrackManager.jsx` fires the trigger via `getChunkConfig`; `GameHUD.tsx` renders a "Journey Complete" overlay (final score, high score, top speed, restart). Resolves the "delta biome unreachable" backlog item — the game now has an authored ending.
- [x] 2026-06-03 — PR #211 merged: runner sprint stamina (Zustand store) + speed-reactive vignette intensity in RunnerVehicle + HUD stamina bar in GameHUD.
- [x] 2026-06-05 — Early-game segments: Glacier Ice Run (-3) and Alpine Wildflower Stream (-2 to -1) authored in `meander_to_waterfall.ts`; `GLACIER_START_INDEX` updated to -3; `startIndex` wired through `TrackManager`/`ChunkManager`; `Experience.jsx` default start now chains glacier → alpine → meander; wildflower spawn boost tied to `particleCount` in `TrackSegment.jsx`; `LEVEL_DESIGN.md` updated with parameters and QA cases.
- [x] 2026-06-03 — PRs #179–181 (Movement Polish + Canyon Expansion epic): raft stamina/brake/dynamic-camera/wake-VFX; slot canyon segment with god rays/mist/biome fix; ScoreSystem.ts fully wired into GameHUD + Experience.jsx (score, multiplier, combo label, high score, top speed)
- [x] 2026-05-27 — npm run build fix: emcc graceful skip in emscripten/build.sh — exits 0 without Emscripten installed (confirmed via .swarm-state.md)
- [x] 2026-05-27 — WatershedWasm buoyancy integration: FloatingObjectManager uses wasmModule.computeBuoyancy() with JS fallback; WatershedWasm.ts uses new Function() trick to avoid Vite static analysis (confirmed via .swarm-state.md, 123 tests pass)
- [x] 2026-05-27 — Reactive audio validation: all 23 MP3 stubs replaced with valid 1-second sine tones (17,180 bytes); AudioDiagnosticsOverlay.tsx DEV-gated overlay; AudioSystem.ts diagnostics APIs (getLoadStatus, getActiveSounds, getAudioContextState); confirmed by .swarm-state.md
- [x] 2026-05-27 — In-scene Level Editor wiring: PR #151 merged; LevelEditor overlay via ?editor=1, useLevelEditor.ts hook + levelEditorValidator.ts
- [x] 2026-05-27 — Downhill creek → second waterfall (segments 23–30): PR #153 merged; meander_to_waterfall.ts extended, RunnerVehicle updated, WaterfallParticles + FlowingWater tuned
- [x] 2026-05-27 — Physics stability: PRs #154–156 — NaN guards on FloatingObjectManager impulses, Set iteration fix on InstancedRigidBodies, XY spawn variation in INITIAL_POINTS
- [x] 2026-05-27 — WASM module (partial): C++/Emscripten module authored in emscripten/ + WatershedWasm.ts bindings + Jest tests; RTTI/Embind conflict resolved (PR #159); binary not yet compiled
- [x] 2026-05-20 — ReactiveAudio NaN volume fix: comprehensive isFinite guards on all volume paths (PR #144)
- [x] 2026-05-20 — R3F Html namespace fix: LoadingDisplay/ErrorDisplay wrapped with drei <Html> to prevent "Div is not part of THREE namespace" error (PR #143)
- [x] 2026-05-20 — Player spawn alignment: spawn position aligned to first canyon chunk, strafe keys mapped to KeyboardControls leftward/rightward, Rapier handle Set iteration fixed (PRs #145–147)
- [x] 2026-05-13 — GameHUD wiring: speedometer, distance counter, wipeout overlay, respawn handler — all criteria met per .swarm-state.md; Experience.jsx renders GameHUD via vehicleRef + isWipeout
- [x] 2026-05-13 — Author segments 0–12: deterministic decoration placement + authored waypoints in meander_to_waterfall.json (PR #130 merged)
- [x] 2026-05-13 — Fix deprecated WebGLRenderTarget constructor for Three.js 0.168+ (PR #131)
- [x] 2026-05-06 — Step 3 — Water Visual Quality: bankFoam via EDGE_FOAM_WIDTH + cameraHeight uniform + proximityScale wave turbulence confirmed in FlowingWater.jsx (commit 80fe84d)
- [x] 2026-05-06 — Step 6 — meander_to_waterfall.json validated end-to-end (PR #125): segments 13–22 fully authored with waypoints, biome overrides, checkpoints; JSONMapManager drives TrackManager
- [x] 2026-04-29 — Step 2 — Terrain Visual Quality: 3-stop vertex color gradient on canyon walls (waterline charcoal → mid grey-brown → rim tan/cream), triplanar UV smoothstep tuned from (0,8) to (6,14), worldNormal varying + moss band tightened to smoothstep(2,4) with normal-based mask in RiverShader.js — committed 7266e6d
- [x] 2026-04-29 — Step 5 — Wire MapSystem.ts into TrackManager (PR #122): DefaultMapManager drives TrackManager via getChunkConfig(), MEANDER_TO_WATERFALL_PROGRESSION config in meander_to_waterfall.ts, JSON stub in meander_to_waterfall.json
- [x] 2026-04-13 — Fix WebGL shader compilation errors (PR #119): double-texture in RiverShader.js, unconditional vertex attribute declarations, floor material missing enableMoss/enableTriplanar flags
- [x] 2026-04-09 — Harden startup rendering / loading routine (PR #117): shader injection fail-safe, geometry validation, process.env → import.meta.env, Mist/SunShafts hash() fix
- [x] 2026-04-09 — Fix production ReferenceError + validation/test coverage (PR #115)
- [x] 2026-04-02 — Upgrade three to ^0.168.0 to satisfy postprocessing peer dep (PR #114)
- [x] 2026-03-19 — Fix vUv2/uv2 conditional guard in RiverShader (PR #113)
- [x] Step 1 — Strip all debug artifacts (green panel, yellow wireframe, hotpink cube, antialias: true, conditional stars) — confirmed clean in App.tsx, Player.jsx, RaftVehicle.tsx, EnhancedSky.jsx
- [x] Step 4 — Post-processing stack: Bloom, Vignette, ChromaticAberration, SSAO, HueSaturation all wired into Experience.jsx via PostProcessingEffects.jsx

- [x] 2026-06-24 — Dispatch E (deploy-pipeline hygiene): confirmed `build_and_patch.py`'s HTML patch was dead code — `vite.config.ts`'s `base: './'` already makes a clean `vite build` emit relative `src="./"`/`href="./"` paths, verified by diffing a fresh build/index.html against the patch logic (zero changes). Removed `patch_html()` and the vestigial `paramiko` install-priming block (leftover from when `deploy.py` was the SFTP script now at `deploy_old.py`; current `deploy.py` only uses `requests`). Verified `deploy.py`'s `build_zip()` walks the build tree generically (no hardcoded asset names/hashes), so it isn't affected by Vite 7's chunk layout. No `--dry-run` flag exists in `deploy.py`/`build_and_patch.py`, so the live deploy path was not exercised. Updated stale doc references in `CLAUDE.md`/`AGENTS.md` that described the now-removed patch step and mischaracterized `deploy.py` as SFTP. Incidentally found a real credential leak (see new Backlog item above) while reading `deploy.py`/`deploy_old.py` for this audit.

## Last run
<!-- Routine writes summary here each run. Overwrites previous. -->
Date: 2026-08-19
Mode: **User Idea** — Noah authored a fresh, explicitly-prioritized board (#369–#375) dated 08-14→08-16 after the entire #353–#357 board landed; it outranks any generated idea. Fix First ruled out: #353–#357 all merged cleanly through PRs #358–#367 (allowlist `[]`, schema debt `[]`, 0 `.js/.jsx`, C++ split, TSL dual-path, survival v2, ghost A+B), **0 open PRs**; Noah's 3 `push fix` commits are hands-on hardening, not broken output.
Focus: **#371 (P0 Foundation) — make navigation honest: retire lying PLAN.md docs, namespace the 79-file `src/systems/` dump, split the 5 oversized hosts (<650 LOC), delete dead duals + boot-time debug leftovers.** One of three P0 Foundations (#371 org/TS, #372 C++, #373 rendering); picked as the mechanical, PR-sliceable, independently-verifiable successor to #353 — the ideal long-horizon swarm target. Every issue claim verified against `main` `c5940e2` (line counts, dead duals, console spam, 0 `.js/.jsx`).
Outcome: **weekly_plan.md updated + dispatch produced.** Verified this session: #353–#357 board fully landed; `untyped-allowlist.json`=`[]`, `levelSchemaDebt.ts` empty, `emscripten/` split into forces/swe/bindings+headers; 5 hosts at 852/783/781/739/712 LOC; 3 stale `PLAN.md`; `systems/PostProcessing.tsx` dead dual; `GameHUD.tsx` boot WASM smoke; Noah's 08-15 debug logs in TrackManager/MapSystem. 7 open issues (#369–#375 — Noah's new board), **0 open PRs**. Plan changes: Today's focus rewritten (User Idea / #371, cascade rationale vs live code); Ideas repointed to #369–#375 with #371 in progress and #353–#357 marked executed→Done; Done gained two reconciliation entries (whole board landed; Noah's push-fix commits); Backlog reconciled (nav-honesty note). Dispatch: kimi-cli swarm = #371 (scoped to `src/systems/**`, `src/components/{FlowingWater.tsx,GameHUD.tsx,TrackSegment/,TrackManager.tsx}`, `src/vehicles/RunnerVehicle/hooks/`, `src/configs/BiomePalettes.ts`, `src/*/PLAN.md`, README + `docs/reference/CODE_HEALTH_GUIDE.md`, `scripts/`; IGNORE `emscripten/**`, `CMakeLists.txt`, `src/rendering/**`, `src/App.tsx`, `src/physics/**` — reserved for #372/#373); Dispatch-B decoupled Copilot issue = #372 Phase A — C++ compile_commands + host-buildable compute TUs + host smoke binary (touches `emscripten/*` + `CMakeLists.txt` + `docs/reference/WASM.md` — zero overlap with kimi's TS/docs surface); Gemini/Kimi/Grok prompts on the C++ toolchain issue; Copilot handoff; Claude Code whole-stack pipeline-health task (pnpm typecheck→test→build→WASM-bundle integrity→`deploy.py --dry-run` zip); Jules wrap-up template for the #371 swarm output; two Gemini review prompts. SECURITY: old `DEPLOY_TOKEN` + `deploy_old.py` plaintext password still in git history (~2 months since code-side fix) — rotation remains owner-only, unchanged.
